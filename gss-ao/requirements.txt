# ============================================================================
# requirements.txt — backend GSS-AO (itération 1)
# Cible : Python 3.11+ (voir .python-version)
# ============================================================================

# --- Validation de schéma / config -------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Parsing documents -------------------------------------------------------
python-docx==1.1.2          # DOCX (CCTP, CCAP, mémoire cadre, BPU)
PyMuPDF==1.24.10            # PDF primaire (fitz) — fontes sous-ensemblées
pdfplumber==0.11.4          # PDF secours + extraction de tables
# Conversion .doc legacy : LibreOffice headless (binaire système, hors pip).
# olefile : utilisé uniquement par le fallback pur-Python (plan B) du rc_parser.
olefile==0.47

# --- OCR (fallback OPTIONNEL, désactivé par défaut) --------------------------
# Tesseract est un binaire système ; pytesseract n'est utile que si OCR activé.
# Décommenter si besoin :
# pytesseract==0.3.13
# Pillow==10.4.0

# --- RAG ---------------------------------------------------------------------
# Vector store : pgvector via SQLAlchemy/psycopg (prod) + fallback JSONL (local).
SQLAlchemy==2.0.35
psycopg[binary]==3.2.3
pgvector==0.3.5

# --- API ---------------------------------------------------------------------
fastapi==0.115.2
uvicorn[standard]==0.31.1
python-multipart==0.0.12    # upload de fichiers (Module A)

# --- LLM (wrapper, non appelé en itération 1) --------------------------------
anthropic==0.37.1

# --- Tests / qualité ---------------------------------------------------------
pytest==8.3.3
ruff==0.6.9
